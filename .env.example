# Copie este arquivo para .env e ajuste os valores antes de executar a aplicação.
# O .env NUNCA deve ser commitado (já está no .gitignore).

# Chave usada para assinar os tokens JWT (gere uma nova com: openssl rand -base64 48)
JWT_SECRET=RsERr2Sn8gFlUuniBeBNhEnw1v6x4Nia/hh1ClVhFOix8XTopXD5+MKe5IkbiKWp

# Diretório onde os arquivos de mídia são gravados (opcional; default: ./storage)
STORAGE_DIR=./storage
