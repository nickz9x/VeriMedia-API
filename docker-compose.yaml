services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_DB=veriMediaDb
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=user
    ports:
      - 5432:5432
    volumes:
      - veriMedia_Data:/var/lib/postgresql/data
volumes:
  veriMedia_Data:
